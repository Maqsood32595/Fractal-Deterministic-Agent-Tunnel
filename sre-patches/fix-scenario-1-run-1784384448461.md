# SRE Fix: DB Connection Saturation

Run ID: 1784384448461
Fix: Upgrade RDS instance class and increase connection pool

```
INSTANCE_CLASS=db.r6g.large
MAX_CONNECTIONS=500
```

Apply with: docker compose up -d --build target-api